/** Map metric modes shared by PriceMap.svelte and /kartta's toggle. */
export type MapMode = 'eur' | 'chg' | 'yld' | 'pir' | 'liq';
export const MAP_MODES: MapMode[] = ['eur', 'chg', 'yld', 'pir', 'liq'];
